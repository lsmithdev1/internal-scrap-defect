import React from 'react';
import ReactDOM from 'react-dom/client';
import CircleDiagram from './circle_diagram';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CircleDiagram />
  </React.StrictMode>
);